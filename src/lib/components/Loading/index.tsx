import { DynamicOptionsLoadingProps } from 'next/dynamic'

// eslint-disable-next-line no-unused-vars
const Loading = (props: DynamicOptionsLoadingProps): React.JSX.Element => <>Loading...</>

export default Loading
